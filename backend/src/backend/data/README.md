# AirForce Sortie Dataset

Place `AirForce_Sortie_Aeromod.csv` in this directory so the telemetry tools
under `backend.src.backend.services.telemetry_tools` can load it. The file is
not bundled with the repo because it may be large, but the agent and telemetry
helpers expect it at runtime.

